"""Media pipeline (Phase B, S5.x): Celery `media` queue + ephemeral GPU provisioning.

S5.0 lands the rails only — queue, provisioner, orchestration loop, spend guardrails.
Real media generation (video S5.1, podcast S5.2) registers tasks here later.
"""
